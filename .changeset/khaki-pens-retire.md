---
"@codex-app/server": patch
---

Expose runtime version metadata through `/health` and `/version`, including git SHA and dirty state, and add automated Changesets-based version management for the repository.
